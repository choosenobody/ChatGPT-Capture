level: extractor
material_learning: true
expected_learning_count: { min: 1, max: 1 }
expected_action: [create_learning]
expected_belief_change: new
expected_topics: [demand_weakness, channel_destocking, open_question]
evergreen:
  expected: false
epistemic:
  must_not_promote_to_fact: true
forbidden:
  - hypothesis_to_fact
description: |
  Uncertain conclusion. The user explicitly does not know. The
  artefact must be `type: question` with resolution-evidence list,
  not a confident claim.
