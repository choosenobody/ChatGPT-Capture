level: extractor
material_learning: true
expected_learning_count: { min: 1, max: 1 }
expected_action: [create_learning, update_entity]
expected_belief_change: unchanged
expected_topics: [VAT, SaaS]
evergreen:
  expected: false
epistemic:
  must_not_promote_to_fact: false
forbidden:
  - hypothesis_to_fact
hard_fail_conditions:
  - hypothesis_promoted
description: |
  A bare tax-rate change is *evidence*, not an evergreen. Must
  NOT be promoted. The right action is `create_learning` + entity
  tag, nothing more.
